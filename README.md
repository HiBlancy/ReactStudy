# ReactStudy

El proyecto de Next.js vive en la carpeta **`game-log/`** (npm no permite el nombre del paquete con mayúsculas en la raíz `ReactStudy`).

```bash
cd game-log
npm run dev
```

Documentación del app: [game-log/README.md](game-log/README.md).
